# Model artifact boundary

Model weights and checkpoints are not stored in Git. Versioned release manifests may be added only after they include checksums, model lineage, supported directions, tokenizer contract, quantization mode, runtime requirements, license records, and measured evidence references.
